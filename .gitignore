# Claude Code local settings — machine-specific permissions, session IDs, absolute paths
.claude/settings.local.json

# MCP config has absolute paths — use .mcp.json.example as the template
.mcp.json

# Monorepo — hoisted node_modules
node_modules/

# Local SQLite runtime files
interview-mcp/data/*.db
interview-mcp/data/*.db-shm
interview-mcp/data/*.db-wal
interview-mcp/data/*.bak
interview-mcp/data/app.db.*
interview-mcp/data/backups/

# Local runtime logs and launcher state
.local/

# macOS metadata
.DS_Store
**/.DS_Store

# java build
target/
